pipeline {
    agent { label 'builder' }
     options {
     lock(label: 'node') 
   
   }
   
    stages {
        stage('Build') {
            steps {
            sleep 60
                sh 'echo "Hello world!"'
            sleep 60
           }
        }
    }
}
