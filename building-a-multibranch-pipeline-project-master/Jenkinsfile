pipeline {
    agent { label 'builder' }
     options {
     lock(label: 'builder1 builder2') 
   
   }
   
    stages {
        stage('Build') {
            steps {
            sleep 60
                sh 'echo "Hello world!"'
            sleep 60
           }
        }
    }
}
