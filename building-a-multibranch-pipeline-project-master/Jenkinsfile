pipeline {
    agent any
    options {
     lock("${currentBuild.getRawBuild().getExecutor().getOwner().getDisplayName()}")
   }
    stages {
        stage('Build') {
            steps {
            sleep 60
           bat 'echo "Hello world!"'
           sleep 60
            }
        }
    }
}   
