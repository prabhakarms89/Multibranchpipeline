pipeline {
    agent any
    properties([[$class: 'BuildBlockerProperty', blockLevel: GLOBAL, blockingJobs: 'blockerbuild/.*', scanQueueFor: ALL, useBuildBlocker: true]])
    stages {
        stage('Build') {
            steps {
                bat 'echo "Hello world!"'
            }
        }
    }
}   
