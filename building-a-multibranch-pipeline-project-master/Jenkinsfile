pipeline {
    agent { label 'builder' }
     options {
    lock("builder1")
   }
    stages {
        stage('Build') {
            steps {
            sleep 60
                sh 'echo "Hello world!"'
            sleep 60
           }
        }
    }
}
