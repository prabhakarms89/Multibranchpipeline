pipeline {
    agent { label 'builder' }
     options {
    lock("builder")
   }
    stages {
        stage('Build') {
            steps {
                bat 'echo "Hello world!"'
            }
        }
    }
}
