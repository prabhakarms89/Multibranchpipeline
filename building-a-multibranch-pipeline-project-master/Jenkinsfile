pipeline {
    agent { label 'builder' }
    /* options {
     lock(label: 'node') 
   
   } */
   
    stages {
    lock(label: 'node'){
        stage('Build') {
            steps {
            sleep 60
                sh 'echo "Hello world!"'
            sleep 60
           }
        }
        }
    }
}
