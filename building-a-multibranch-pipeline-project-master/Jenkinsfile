pipeline {
    options {
     lock(label: 'node') 
   
   }
    agent { label 'builder' }
     
 //  lock(label: 'node'){
    stages {
    
        stage('Build') {
            steps {
            sleep 60
                sh 'echo "Hello world!"'
            sleep 60
           }
        }
      }
}
