pipeline {
    agent { label 'builder' }
     options {
    lock(label: 'jaffanode2', label: 'jaffanode')
   }
    stages {
        stage('Build') {
            steps {
            sleep 60
                sh 'echo "Hello world!"'
            sleep 60
           }
        }
    }
}
