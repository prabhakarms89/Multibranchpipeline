pipeline {
    agent { label 'builder' }
     options {
    lock (resource: ['test01', 'test02'])
   }
    stages {
        stage('Build') {
            steps {
            sleep 60
                sh 'echo "Hello world!"'
            sleep 60
           }
        }
    }
}
